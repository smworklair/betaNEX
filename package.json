{
  "name": "betanex",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm -C web dev",
    "build": "pnpm -C web build",
    "preview": "pnpm -C web preview",
    "lint": "pnpm -C web lint"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "@google/genai",
      "protobufjs"
    ]
  }
}
