// Пакет tenancy изолирует данные организаций (tenant'ов).
//
// Модель (веха M2): каждая доменная строка несёт tenant_id; изоляция
// обеспечивается двумя рубежами:
//
//  1. tenant-контекст протаскивается из HTTP-middleware через команды
//     до SQL (SET LOCAL app.tenant_id в каждой транзакции);
//  2. Row-Level Security в Postgres — второй, страхующий рубеж.
//
// Ни один запрос к доменным данным не выполняется без tenant-контекста.
package tenancy
