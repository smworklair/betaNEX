// Пакет audit — append-only журнал действий.
//
// Каждая команда, прошедшая через шину, оставляет запись: актор, tenant,
// имя команды, результат (успех/отказ/ошибка), решение авторизации,
// момент времени и trace-id. Запись создаётся в той же транзакции,
// что и само изменение, — аудит не может разойтись с данными.
//
// Журнал только пополняется: записи не изменяются и не удаляются.
// Наполнение — веха M2.
package audit
