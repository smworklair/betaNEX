// Package auth — аутентификация ядра NEX (ADR-004): собственные
// server-side сессии с opaque-токенами и пароли argon2id.
//
// Решения, зафиксированные архитектурой:
//   - сессии хранятся на сервере и отзываются мгновенно — JWT как
//     сессии отклонён сознательно;
//   - в БД лежит только sha256-хэш токена: утечка БД не раскрывает
//     действующих сессий;
//   - пароли — argon2id в формате PHC с параметрами по рекомендации
//     OWASP;
//   - пакет не знает про HTTP: транспорт (cookie, endpoints) живёт в
//     platform/httpapi, хранилище — в platform/postgres.
package auth
