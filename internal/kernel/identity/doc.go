// Package identity отвечает на вопрос «кто актор».
//
// Зона ответственности (веха M3):
//   - пользователи и их учётные данные (пароли — только argon2id-хэши);
//   - сессии: opaque-токены, хранение на сервере, ротация и отзыв;
//   - аутентификация запроса: токен из cookie → актор в контексте.
//
// Вне зоны ответственности: права доступа (authz), принадлежность
// к tenant'у (tenancy). Identity лишь устанавливает личность.
package identity
