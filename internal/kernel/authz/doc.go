// Package authz отвечает на вопрос «что актору разрешено».
//
// Модель (веха M4): RBAC со скоупом tenant'а — актору назначаются роли,
// роль — это именованный набор прав (permission). Проверка выполняется
// в шине команд до исполнения команды, а не в HTTP-хендлерах: у каждой
// команды объявлено требуемое право.
//
// Решения авторизации (разрешено/отказано и почему) отражаются в аудите.
// Политики — обычный Go-код, покрываемый юнит-тестами (см. ADR-005).
package authz
