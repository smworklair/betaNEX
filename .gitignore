# Node / frontend prototype
node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
# pnpm store, если случайно создастся в корне (см. compose.yaml:web — в
# норме store живёт в именованном docker-томе, не в репозитории).
.pnpm-store/

# Прод-деплой (см. deploy/README.md): секреты и собранная статика
# фронтенда — пересобираются на сервере, не артефакт для git.
deploy/.env
deploy/web-dist/

# Go / NEX backend
/bin/
/tmp/
*.test
__pycache__/
